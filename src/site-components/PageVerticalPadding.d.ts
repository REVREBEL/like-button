import * as React from "react";
import * as Types from "./types";

declare function PageVerticalPadding(
    props: {
        as?: React.ElementType;
        paddingPaddingVertical?: "Padding None" | "Padding 2x" | "Padding 4x" | "Padding 6x" | "Padding 8x";
        slotSlot?: Types.Slots.SlotContent;
    }
): React.JSX.Element