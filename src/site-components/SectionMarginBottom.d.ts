import * as React from "react";
import * as Types from "./types";

declare function SectionMarginBottom(
    props: {
        as?: React.ElementType;
        slotSlot?: Types.Slots.SlotContent;
        marginMarginBottom?: "Bottom Margin None" | "Bottom Margin 2x" | "Bottom Margin 4x" | "Bottom Margin 6x" | "Bottom Margin 8x";
    }
): React.JSX.Element