import * as React from "react";
import * as Types from "./types";

declare function PageLayoutTemplateBasic(
    props: {
        as?: React.ElementType;
        slot?: Types.Slots.SlotContent;
        slot?: Types.Slots.SlotContent;
        slot?: Types.Slots.SlotContent;
        slot?: Types.Slots.SlotContent;
        slot?: Types.Slots.SlotContent;
        slot?: Types.Slots.SlotContent;
    }
): React.JSX.Element